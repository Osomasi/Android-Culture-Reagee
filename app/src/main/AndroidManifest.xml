<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cs2001_group10">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AdminQuestions"></activity>
        <activity android:name=".AdminAddQuestion" />
        <activity android:name=".AdminUpdateQuestion" />
        <activity android:name=".AdminShowQuestion" />
        <activity android:name=".AdminRequest" />
        <activity android:name=".Questions" />
        <activity android:name=".SelectTopic" />
        <activity android:name=".SettingsMenu" />
        <activity android:name=".HomeScreen" />
        <activity android:name=".NewUser" />
        <activity android:name=".SetTimer" />
        <activity android:name=".Topics" />
        <activity android:name=".SelectApps" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".PopupService" />
    </application>

</manifest>